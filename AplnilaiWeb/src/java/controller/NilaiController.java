package controller;
import koneksi.KoneksiDB;
import java.sql.PreparedStatement;

public class NilaiController {
    public void simpan(model.Nilai n) {
        // Logika INSERT ke database menggunakan KoneksiDB.getConnection()
    }
}